📄 Interview Agent — Technical Rationale & Design Explanation

(Готовая вставка в документацию MindForge)

1. Overview

InterviewAgent реализует интеллектуальный модуль многоагентной системы KM-6, предназначенный для проведения интервью, оценки ответов кандидата и динамической адаптации сложности вопросов. Агент изолирован от LLM и работает через UAG, поддерживая Zero-Trust модель MindForge.

2. Architecture Principles

Агент построен по принципам:

Determinism for Testing — поведение воспроизводимо для CI/CD.

Dependency Injection — KR API абстрагирован через интерфейс.

Isolated Reasoning — агент не вызывает LLM напрямую.

Stateless Core Logic — вычисления предсказуемы.

MSDLC Documentation-Driven Development — сначала спецификация, тесты, затем код.

3. Components and Responsibilities
3.1 Question Generation

Вопросы хранятся в структуре QUESTION_BANK по ролям и уровням сложности (L1–L6).
Метод:

generate_question(role, level)


✔ выбирает вопрос для конкретного уровня
✔ гарантирует детерминированный выход (важно для unit-тестов)
✔ поддерживает расширение за счёт конфигов/файлов

3.2 Answer Evaluation

Метод:

evaluate_answer(answer, context)


оценивает ответ по пяти независимым метрикам:

Metric	Logic	Weight
Correctness	Наличие технических ключевых слов	0.35
Depth	Количество смысловых элементов в ответе	0.25
Structure	Логическая связность, пунктуация	0.15
Relevance	Сопоставление с контекстом KR API	0.15
Reasoning	Наличие причинно-следственных конструкций	0.10

Каждая метрика независима и минимально вычислительно затратна.

Плюсы данного подхода:

работает без LLM

дешёвая оценка (O(n) по длине строки)

легко покрывается тестами

избегает риска галлюцинаций

Оценка нормализуется в диапазон 0.0–1.0.

3.3 Difficulty Adjustment

Метод:

adjust_difficulty(score, current_level)


реализует адаптивную сложность:

≥ 0.8 → усложнить вопрос

< 0.5 → упростить

иначе → сохранить уровень

Это обеспечивает индивидуальную адаптацию под реальные знания кандидата.

3.4 Session Report

Метод:

build_report(history)


формирует аналитический отчёт:

итоговый балл

сильные стороны

зоны для улучшения

история вопросов и ответов

Отчёт можно экспортировать в PDF/JSON/YAML.

4. run_step() — Unified Interaction Interface

Метод:

run_step(role, level, answer, history)


представляет собой универсальную точку входа:

Если ответа нет → сгенерировать вопрос.

Если ответ есть →

получить контекст через KR API

вычислить оценку

обновить уровень сложности

вернуть следующий вопрос

Этот интерфейс идеально подходит для:

FastAPI

Telegram-handlers

UAG capabilities

многоагентных оркестраторов

5. KR API Integration Rationale

Агент НЕ использует LLM для:

генерации вопросов

оценки ответов

принятия решений

Он полагается на:

✔ KR API → контекст из корпоративной базы знаний
✔ UAG → правила политик
✔ LLM → только на финальном этапе reasoning (если включено)

Это соответствует Zero-Trust AI-модели:

агенты не доверяют контенту

агенты не имеют доступа к сырым данным

агенты не имеют прямого доступа к LLM

всё проходит через политический слой UAG

6. Why Heuristic Evaluation (not LLM scoring)?

LLM-оценка нестабильна:

может галлюцинировать

не воспроизводима → CI тесты ломаются

требует сетевых запросов

не обеспечивает безопасность данных

Поэтому:

✔ на уровне L3 применяется предсказуемая эвристика
✔ на уровне L4+ можно включить LLM-scoring через конфиг
7. Extensibility (Built-In Future-Proofing)

Решение уже поддерживает:

добавление новых ролей

кастомный банк вопросов

расширенные отчёты

замена эвристики на ML-оценку

семантический поиск контекста

интеграцию через Orchestrator KM-6

историю сессий

⚡ 8. Summary of Design Decisions
Выбор	Обоснование
Детерминированная генерация вопросов	100% предсказуемость тестов
Простая эвристика оценки	скорость, безопасность, отсутствие LLM-зависимости
UAG → Strict Zero Trust	корпоративный уровень защиты
KR API как источник знаний	единый источник правды
Stateless агент	лёгкая масштабируемость
Unified run_step	абстракция для любого интерфейса (бот, API, агенты)
📌 9. Conclusion

InterviewAgent спроектирован:

как часть MindForge KM-6

как L3-совместимый интеллектуальный модуль

с полным соответствием MSDLC

с чистым разделением ответственности

с безопасной моделью данных

с прозрачностью для тестирования

с расширяемой архитектурой

Он уже готов:

к интеграции с Telegram-ботом

к работе через UAG

к подключению через Orchestrator

к промышленных сценариям HR-интервью