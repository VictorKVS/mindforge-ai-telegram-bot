# 🔒 Security Model — Zero-Trust AI Implementation

MindForge AI Telegram Bot реализует **первую полноценную модель защиты для LLM-ботов**.

---

# 1. Модель безопасности MindForge (уровни L1–L7)

### **L1 — Input Filtering**
Блокировка:
- jailbreak prompts (DAN, Dev Mode, Do Anything Now)
- SQL/OS patterns
- base64/rot13 обфускации
- high-entropy payloads (>4.5 bits/char)

### **L2 — RBAC/ABAC**
Каждый пользователь имеет:
- роль  
- атрибуты доступа  
- уровень доверия  

### **L3 — Policy Engine**
Примеры политик:
- "Запрещена техническая диагностика"  
- "Максимальная длина ответа 400 символов"  

### **L4 — Validation / Sanitization**
Очищаем:
- URL  
- команды  
- вредоносные запросы  

### **L5 — Sandbox Mode**
Если запрос подозрительный → ограниченный режим → частичное выполнение.

### **L6 — Competence Control**
Модель не выходит за пределы компетенций:
- если нет уверенности → уточнение или отказ.

### **L7 — Audit & Governance**
- лог без PII  
- интеграция с MindForge.GRC  
- анализ аномалий  

---

# docs/rag.md
```markdown
# 📚 RAG Architecture

RAG в MindForge AI Telegram Bot создан для работы:

- с файлами
- документацией
- корпоративной базой знаний
- справочными материалами

## 🔹 Pipeline

1. Нормализация документа  
2. Разбиение на чанки (256–512 токенов)  
3. Embeddings → векторная БД  
4. Поиск  
5. Контекстная сборка  
6. Ответ LLM  

---

# docs/api.md
```markdown
# 🧩 API Interface

Этот документ описывает **внутренние API-интерфейсы** проекта.

---

## 1. LLM Client

### POST /ai/generate
```json
{
  "prompt": "Your question",
  "context": "optional"
}
