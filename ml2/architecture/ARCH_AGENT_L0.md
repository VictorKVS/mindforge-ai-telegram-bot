ARCH_AGENT_L0.md
Agent L0 — Baseline AI Agent Architecture
1. Purpose / Назначение

Agent L0 — это базовый уровень AI-агента в экосистеме MindForge, предназначенный для:

обработки пользовательских запросов,

выполнения reasoning с использованием LLM,

формирования структурированных намерений (intents),

взаимодействия с внешним миром исключительно через UAG.

Agent L0 — это «мыслящий исполнитель»,
но не автономный субъект и не владелец доступа.

2. Architectural Position / Положение в системе

Agent L0 располагается между пользовательским интерфейсом и контрольным слоем UAG.

User / Telegram / UI
        ↓
Agent L0 (LLM + Reasoning)
        ↓   structured intent
UAG (access control & enforcement)
        ↓
Providers (shop, citizen, services)

Ключевой принцип

Agent никогда не взаимодействует с провайдерами напрямую

Любое действие агента проходит через UAG

Agent не знает:

где находятся данные,

как они хранятся,

как реализованы действия.

3. Scope / Область ответственности
Agent L0 отвечает за:

интерпретацию пользовательского запроса,

построение reasoning-цепочки,

выбор допустимого intent,

формирование корректного запроса к UAG,

интерпретацию ответа (OK / DENY),

корректную реакцию на отказ.

Agent L0 НЕ отвечает за:

контроль доступа,

выполнение действий,

хранение данных,

принятие бизнес-решений,

управление политиками безопасности.

4. Functional Model / Функциональная модель
4.1 Processing Pipeline
Input (text)
   ↓
Intent Recognition
   ↓
Reasoning (LLM)
   ↓
Intent Selection
   ↓
Contract Assembly
   ↓
UAG Call
   ↓
Response Interpretation
   ↓
User Output

4.2 Intent-driven behavior

Agent L0 работает исключительно в intent-модели.

Примеры допустимых intent’ов (L0):

get_price

get_info

search

Agent не может выполнять произвольные действия.
Только те, которые выражены через intent.

5. Reasoning Model
5.1 Nature of reasoning

Reasoning выполняется внутри агента

Может использовать:

Chain-of-Thought

ReAct

Plan-Act

Reasoning не выходит за пределы агента

Reasoning не влияет напрямую на доступы

Reasoning = мышление, не действие.

5.2 Observability

Reasoning может быть:

логируемым (в sandbox),

отключённым (в prod),

частично маскируемым.

6. Interaction with UAG
6.1 Mandatory Gateway Rule

Agent L0 обязан взаимодействовать с внешним миром
только через UAG.

Любая попытка:

прямого доступа,

обхода,

повторного запроса после DENY

рассматривается как нарушение архитектуры.

6.2 Request Structure (conceptual)
agent_id
intent
target
query
context


Контракты определены в:

CONTRACT_POLYGON_TRAINING_L1.md

CONTRACT_UAG_ACCESS_L1.md

7. DENY Handling Policy
7.1 Mandatory behavior on DENY

При получении DENY Agent L0 обязан:

прекратить попытки выполнения действия,

не эскалировать intent,

сформировать корректное уведомление пользователю,

зафиксировать отказ в логах.

7.2 Forbidden behavior

Запрещено:

повторять запрос с изменёнными параметрами,

пытаться подобрать intent,

использовать альтернативные пути доступа.

8. State Model (L0)

Agent L0 является квази-статeless.

Допустимые состояния:

IDLE

PROCESSING

WAITING_FOR_UAG

RESPONDING

Agent не хранит долгосрочное состояние, влияющее на доступ.

9. Security Principles

Agent не содержит секретов доступа

Agent не знает ключей API

Agent не хранит персональные данные

Agent не принимает решений о правах

Agent = zero-trust component.

10. Relationship with Polygon

Agent L0 подлежит обязательной проверке в Polygon

Без PASS от Polygon агент:

не регистрируется в PROD-UAG,

не получает доступа к реальным провайдерам.

Polygon проверяет:

соблюдение Mandatory Gateway Rule,

корректную работу с DENY,

соблюдение контрактов.

11. Evolution Path
L0 → L1 (future)

Возможные расширения:

управляемая память,

multi-intent reasoning,

delegation (через UAG),

orchestration с другими агентами.

При этом:

L0-инварианты сохраняются.

12. Architectural Status

Document status: PROPOSED

Target level: L0

Change authority: Chief Architect / Project Owner

13. Final Architectural Principle

Agent may think freely,
but may act only through UAG.

🔒 End of Document