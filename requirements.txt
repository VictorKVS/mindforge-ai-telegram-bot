# =========================
# Core runtime (Python L1)
# =========================

# YAML (contracts, curriculum, scenarios)
PyYAML>=6.0

# JSON Schema validation (UAG enforcement)
jsonschema>=4.0.0

# =========================
# Optional / Legacy support
# (у тебя уже используется)
# =========================

# Если используется Telegram Bot API
python-telegram-bot>=20.0

# Если используется HTTP (API / providers / future UAG)
requests>=2.31.0

# =========================
# Development / Debug (L1)
# =========================

# Для удобной отладки и логов (опционально)
rich>=13.0

aiogram==3.8.0
python-dotenv
