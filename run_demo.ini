# run_demo.ps1
Write-Host "🚀 MindForge DEMO launcher"

# 1. Проверка Python
$python = py -3.12
if ($LASTEXITCODE -ne 0) {
    Write-Error "Python 3.12 not found. Install Python 3.12 and retry."
    exit 1
}

# 2. Создание venv (если нет)
if (-Not (Test-Path ".venv")) {
    Write-Host "📦 Creating virtual environment..."
    py -3.12 -m venv .venv
}

# 3. Установка зависимостей
Write-Host "📥 Installing dependencies..."
.\.venv\Scripts\pip.exe install -r requirements.txt

# 4. Запуск бота
Write-Host "🤖 Starting Telegram bot (DEMO mode)..."
.\.venv\Scripts\python.exe -m src.bot.bot --demo
